# agate: Very simple server for the Gemini hypertext protocol
# https://github.com/mbrubeck/agate

services:
  agate:
    restart: unless-stopped
    build: /opt/docker/gemini/Dockerfile
    container_name: agate
    volumes:
    - /opt/docker/agate/content:/var/agate/content
    - /opt/docker/agate/certs:/var/agate/certs
    ports:
    - "1965:1965"
    command: >
      --content content --certs certs --addr 0.0.0.0:1965
      --hostname gmi.runtimeterror.dev
      --hostname capsule.jbowdre.lol
      --lang en-US
